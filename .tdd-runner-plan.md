# RUNNER ENFORCEMENT (MANDATORY)

You must execute strict Outside-In TDD and prove it.
- First, write failing OUTER acceptance tests for AC anchors.
- Then implement via minimal inner loops; do not batch large implementation before tests.
- If the story has user-flow/API journey ACs and the test framework supports it, outer acceptance tests must cover that boundary.
- If an outer acceptance test cannot execute due to an environment or sandbox constraint (e.g. browser launch blocked, live server unavailable, OS-level permission denied) — document the reason in `issues`, proceed with TDD with unit tests instead, and set `tests_passed` based on the unit tests that did run. Do not retry the blocked test.
- `tests_ran` and `tests_passed` must be booleans (true/false), not lists.
- Print a loop ledger line for each loop in runner output: `[TDD-LOOP <n>] <red->green summary>`.
- At the end, write `./.tdd-verdict.json` (relative to cwd) with at least:
  `tdd_followed`, `tests_ran`, `tests_passed`, `issues`, `confidence`, `tdd_loops_completed`, `outer_loops_completed`.
- `tdd_loops_completed` MUST be >= 1 (one or more loops per AC anchor).
- Do NOT attempt to run the full test suite — it takes too long and must run in CI.
- Run ONLY the specific test file(s) you created or modified and confirm they pass.

Failure to meet this is a hard pipeline gate and will trigger automatic retry.

---

# STORY-014 (SCRUM-584) — Preset GraphQL API Contract

## Files to create or modify

| File Path | Action | Purpose |
|-----------|--------|---------|
| `packages/twenty-server/src/modules/dashboard/dtos/dashboard-filter-preset.dto.ts` | Create | GraphQL output DTO for preset contract |
| `packages/twenty-server/src/modules/dashboard/dtos/inputs/create-dashboard-filter-preset.input.ts` | Create | Input DTO for preset creation mutation |
| `packages/twenty-server/src/modules/dashboard/dtos/inputs/update-dashboard-filter-preset.input.ts` | Create | Input DTO for preset update mutation |
| `packages/twenty-server/src/modules/dashboard/resolvers/dashboard.resolver.ts` | Modify | Add preset CRUD queries and mutations |
| `packages/twenty-server/src/modules/dashboard/dashboard.module.ts` | Modify | Register preset resolver and service |
| `packages/twenty-server/src/modules/dashboard/services/dashboard-filter-preset.service.ts` | Create | Service stub for preset operations |
| `packages/twenty-server/src/modules/dashboard/entities/dashboard-filter-preset.entity.ts` | Create | Entity stub for preset persistence |

## Acceptance Test Anchors

| AC-ID | Outer Test Description | File |
|-------|----------------------|------|
| AC-001 | GraphQL mutation `createDashboardFilterPreset` returns created preset with id | `dashboard.resolver.spec.ts` |
| AC-002 | GraphQL query `dashboardFilterPresets(dashboardId)` returns list of presets | `dashboard.resolver.spec.ts` |
| AC-003 | GraphQL mutation `updateDashboardFilterPreset` returns updated preset | `dashboard.resolver.spec.ts` |
| AC-004 | GraphQL mutation `deleteDashboardFilterPreset` returns success | `dashboard.resolver.spec.ts` |
| AC-005 | Read-only user calling create/update/delete mutations receives permission error | `dashboard.resolver.spec.ts` |

## Implementation Order (Outside-In TDD — Double Loop)

**Context-aware boundary:** This is a backend GraphQL API story. Outer boundary is resolver-level integration tests using NestJS testing utilities. Inner boundary is service unit tests. The repository layer below the service is explicitly stubbed per story requirements.

### Outer loop — AC-001: Create Preset

1. Write failing resolver test asserting `createDashboardFilterPreset` mutation returns preset with `id`, `name`, `dashboardId`, `filterState` fields. Run it. Confirm FAILS (assertion error, not import error).
2. **Inner loop** until outer test passes:
   - a. Create `DashboardFilterPresetDto` with `@Field()` decorators for all output fields
   - b. Create `CreateDashboardFilterPresetInput` with `@Field()` for `name`, `dashboardId`, `filterState`
   - c. Create stub `DashboardFilterPresetService` with `create()` method returning mock preset
   - d. Create stub `DashboardFilterPresetEntity` (minimal TypeORM decorator setup)
   - e. Add `createDashboardFilterPreset` mutation to `DashboardResolver`, inject service, wire to service call
   - f. Register service and entity in `DashboardModule`
3. When outer test passes, commit: `feat: implement create preset mutation (AC-001)`

### Outer loop — AC-002: List Presets

1. Write failing resolver test asserting `dashboardFilterPresets(dashboardId)` returns array of presets. Run it. Confirm FAILS.
2. **Inner loop** until outer test passes:
   - a. Add `findByDashboardId()` method to service stub
   - b. Add `dashboardFilterPresets` query to resolver, wire to service
3. When outer test passes, commit: `feat: implement list presets query (AC-002)`

### Outer loop — AC-003: Update Preset

1. Write failing resolver test asserting `updateDashboardFilterPreset` mutation returns updated preset. Run it. Confirm FAILS.
2. **Inner loop** until outer test passes:
   - a. Create `UpdateDashboardFilterPresetInput` with `id` (required) and optional `name`, `filterState`
   - b. Add `update()` method to service stub
   - c. Add `updateDashboardFilterPreset` mutation to resolver
3. When outer test passes, commit: `feat: implement update preset mutation (AC-003)`

### Outer loop — AC-004: Delete Preset

1. Write failing resolver test asserting `deleteDashboardFilterPreset` mutation returns success boolean. Run it. Confirm FAILS.
2. **Inner loop** until outer test passes:
   - a. Add `delete()` method to service stub
   - b. Add `deleteDashboardFilterPreset` mutation to resolver
3. When outer test passes, commit: `feat: implement delete preset mutation (AC-004)`

### Outer loop — AC-005: Permission Enforcement

1. Write failing resolver test asserting read-only user receives `ForbiddenException` on create/update/delete. Run it. Confirm FAILS.
2. **Inner loop** until outer test passes:
   - a. Create `DashboardPresetAccessService` stub for permission checks
   - b. Inject workspace auth context (use existing `AuthUser` decorator pattern from codebase)
   - c. Add guard logic in resolver methods checking `canUpdateObjectRecords` permission
   - d. Add unit tests for permission edge cases: read-only blocked, editor allowed, admin allowed
3. When outer test passes, commit: `feat: implement preset permission enforcement (AC-005)`

### Final Verification

Run full test suite in twenty-server package: `npx nx test twenty-server --testPathPattern=dashboard`

Write TDD Verdict file at `/tmp/STORY-014-tdd-verdict.json`:
```json
{
  "storyId": "STORY-014",
  "tdd_followed": true,
  "tests_ran": true,
  "tests_passed": true,
  "issues": "",
  "confidence": "high",
  "tdd_loops_completed": 5,
  "outer_loops_completed": 5
}
```

## Open questions or assumptions

1. **Assumption**: Filter state payload format matches existing `ChartFilter` structure (`recordFilters` + `recordFilterGroups`) per EPIC discovery report.
2. **Assumption**: Permission check leverages existing workspace auth context and `objectPermissions.canUpdateObjectRecords` pattern from `DashboardActionsConfig.tsx`.
3. **Assumption**: Repository layer is stubbed; actual persistence implementation is STORY-015 scope.
4. **Assumption**: GraphQL type definitions follow existing Twenty patterns: `ID` for ids, nullable arrays for lists, `@Args()` for inputs.
5. **Open**: Whether preset names must be unique per dashboard — assuming no uniqueness constraint for this story (enforced in persistence layer if needed).
