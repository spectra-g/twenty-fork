# RUNNER ENFORCEMENT (MANDATORY)

You must execute strict Outside-In TDD and prove it.
- First, write failing OUTER acceptance tests for AC anchors.
- Then implement via minimal inner loops; do not batch large implementation before tests.
- If the story has user-flow/API journey ACs and the test framework supports it, outer acceptance tests must cover that boundary.
- If an outer acceptance test cannot execute due to an environment or sandbox constraint (e.g. browser launch blocked, live server unavailable, OS-level permission denied) — document the reason in `issues`, proceed with TDD with unit tests instead, and set `tests_passed` based on the unit tests that did run. Do not retry the blocked test.
- `tests_ran` and `tests_passed` must be booleans (true/false), not lists.
- Print a loop ledger line for each loop in runner output: `[TDD-LOOP <n>] <red->green summary>`.
- At the end, write `./.tdd-verdict.json` (relative to cwd) with at least:
  `tdd_followed`, `tests_ran`, `tests_passed`, `issues`, `confidence`, `tdd_loops_completed`, `outer_loops_completed`.
- `tdd_loops_completed` MUST be >= 3 (one or more loops per AC anchor).
- Do NOT attempt to run the full test suite — it takes too long and must run in CI.
- Run ONLY the specific test file(s) you created or modified and confirm they pass.

Failure to meet this is a hard pipeline gate and will trigger automatic retry.

---

# Implementation Plan: STORY-014 (SCRUM-593) - Create Dashboard Filter Preset

## 1. Files to Create or Modify

| File Path | Action | Purpose |
|-----------|--------|---------|
| `packages/twenty-server/src/modules/dashboard/dtos/dashboard-filter-preset.dto.ts` | Create | GraphQL DTO for preset response with id, name, dashboardId, filter, and timestamps |
| `packages/twenty-server/src/modules/dashboard/dtos/inputs/create-dashboard-filter-preset.input.ts` | Create | Input type for createDashboardFilterPreset mutation with validation decorators |
| `packages/twenty-server/src/modules/dashboard/services/dashboard-filter-preset.service.ts` | Create | Service layer implementing createPreset with validation, persistence, and error handling |
| `packages/twenty-server/src/modules/dashboard/services/__tests__/dashboard-filter-preset.service.spec.ts` | Create | Unit tests for service layer covering success, validation errors, and not-found scenarios |
| `packages/twenty-server/src/modules/dashboard/resolvers/dashboard.resolver.ts` | Modify | Add createDashboardFilterPreset mutation resolver, wire to service, apply guards |
| `packages/twenty-server/src/modules/dashboard/dashboard.module.ts` | Modify | Register DashboardFilterPresetService as provider and export if needed |

## 2. Acceptance Test Anchors

| AC-ID | Outer Test Description | File |
|-------|----------------------|------|
| AC-001 | GraphQL mutation `createDashboardFilterPreset` with valid input returns persisted preset with generated ID | `packages/twenty-server/src/modules/dashboard/resolvers/dashboard.resolver.spec.ts` or integration test at resolver boundary |
| AC-002 | GraphQL mutation with invalid filter structure returns validation error (400/422) with descriptive message | Same resolver test file - error path scenario |
| AC-003 | GraphQL mutation for non-existent dashboardId returns not-found error (404) | Same resolver test file - error path scenario |

**Note:** Outer loop tests are at the GraphQL resolver boundary (integration-style tests using NestJS testing utilities). Inner loop unit tests for service layer logic are detailed in implementation order below.

## 3. Implementation Order (Outside-In TDD — Double Loop)

### Context-Aware Boundary
Test environment has Jest configured for the server package. Acceptance tests will use NestJS `Test.createTestingModule` with mocked repository dependency (from inner-layer stub). No new test frameworks required.

### AC-001: Valid Preset Creation (Outer Loop)

1. **Write failing acceptance test** in resolver test file:
   - Test `createDashboardFilterPreset` mutation with valid `dashboardId`, `name`, and `filter` input
   - Assert response contains `id`, `name`, `dashboardId`, `filter`, `createdAt`, `updatedAt`
   - Run test: expect failure (resolver method doesn't exist or returns undefined)

2. **Inner Loop — Resolver Layer:**
   - Write failing unit test: resolver calls service.create with correct args
   - Implement resolver method stub that delegates to (non-existent) service
   - Run test: expect failure (service not found)

3. **Inner Loop — Service Layer:**
   - Write failing unit test: service.create validates input and calls repository.save
   - Implement DashboardFilterPresetService with `createPreset` method
   - Inject repository stub from dependency story
   - Implement validation: check dashboard exists via repository or query
   - Implement persistence: save preset via repository stub
   - Run test: expect PASS

4. **Inner Loop — DTO/Input:**
   - Write failing test: Input DTO validates required fields (`name`, `dashboardId`, `filter`)
   - Implement `CreateDashboardFilterPresetInput` with class-validator decorators
   - Implement `DashboardFilterPresetDTO` for response shape
   - Run test: expect PASS

5. **Wire Module:**
   - Add service to dashboard.module.ts providers
   - Ensure repository stub is available for injection
   - Run outer acceptance test: expect PASS

### AC-002: Invalid Filter Structure Validation (Outer Loop)

1. **Write failing acceptance test:**
   - Test mutation with malformed `filter` (e.g., invalid operator, missing required fields)
   - Assert GraphQL validation error or service-level ValidationException

2. **Inner Loop:**
   - Write failing unit test: service.validateFilter rejects invalid structure
   - Implement filter structure validation using existing `ChartFilter` or similar shared type validation
   - Reference: `configuration.filter` pattern from chart widgets (`{ recordFilters?, recordFilterGroups? }`)
   - Run test: expect PASS
   - Run outer test: expect PASS

### AC-003: Non-Existent Dashboard Error (Outer Loop)

1. **Write failing acceptance test:**
   - Test mutation with `dashboardId` that doesn't exist in database
   - Assert NotFoundException or equivalent GraphQL error

2. **Inner Loop:**
   - Write failing unit test: service.createPreset throws when dashboard lookup fails
   - Implement dashboard existence check in service (query by id)
   - Throw NotFoundException with descriptive message if dashboard not found
   - Run test: expect PASS
   - Run outer test: expect PASS

### Additional Unit Test Coverage (Edge Cases)

- **Name validation:** Empty string, too long (>255 chars), special characters
- **Filter empty state:** Valid empty filter object vs null/undefined handling
- **Duplicate names:** Optional - check if story scope includes unique name constraint per dashboard
- **Repository failure:** Handle database/repository errors gracefully

### Commit Message Pattern
- `feat: add DashboardFilterPresetDTO and CreateDashboardFilterPresetInput (AC-001)`
- `feat: implement DashboardFilterPresetService with createPreset (AC-001)`
- `feat: wire createDashboardFilterPreset resolver (AC-001)`
- `feat: add filter structure validation (AC-002)`
- `feat: add dashboard existence check (AC-003)`

### MANDATORY FINAL STEP — TDD Verdict

After all outer loops complete, run the full test suite:
```bash
# From repo root
cd /Users/girish/IdeaProjects/clawdence/repos/twenty && mise exec -- yarn test packages/twenty-server/src/modules/dashboard
```

Then write the TDD verdict file at `/tmp/STORY-014-tdd-verdict.json`:
```json
{
  "storyId": "STORY-014",
  "tdd_followed": true,
  "tests_ran": true,
  "tests_passed": true,
  "issues": "",
  "confidence": "high",
  "tdd_loops_completed": 8,
  "outer_loops_completed": 3
}
```

## 4. Open Questions or Assumptions

1. **Repository Stub Dependency:** Assumes STORY for "Repository Stub for Dashboard Presets" has created the repository interface/entity. The service will inject this repository and call `save()` and `findByDashboardId()` methods.

2. **Filter Type Definition:** Assumes existing shared types for filter structures (like `ChartFilter` from `configuration.filter`) can be reused or adapted for preset filter field. If not available, a new validation schema will be defined inline in the input DTO.

3. **Dashboard Existence Check:** Assumes dashboard repository or query service is available to verify `dashboardId` exists before creating preset. If not, will need to inject dashboard repository dependency.

4. **Unlimited Presets:** Per epic requirements, no limit on preset count per dashboard. No count validation needed in create flow.

5. **User/Permission Context:** Assumes authentication guards are already applied at resolver level (existing pattern). Service layer receives user context via decorator/argument if needed for audit fields.
